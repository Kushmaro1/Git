import { CustomersRouting.Module } from './customers-routing.module';

describe('CustomersRouting.Module', () => {
  it('should create an instance', () => {
    expect(new CustomersRouting.Module()).toBeTruthy();
  });
});
