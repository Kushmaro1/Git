import { ProductsRoutingModule } from './products-routing-module';

describe('ProductsRoutingModule', () => {
  it('should create an instance', () => {
    expect(new ProductsRoutingModule()).toBeTruthy();
  });
});
